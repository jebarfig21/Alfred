<?php
require_once "core/BaseController.php";

	class SensoresController extends BaseController {
		//private $medicion,$nodo;

		public function __construct(){
			parent::__construct();

		}

		public function index(){

		}

		public function getHumedad(){
			/*Regresa un arreglo donde los elemetos son (nodo,humedad)*/
			$nodosModel = new Node();
			$nodos = $nodosModel->getAll();
			foreach($nodos as $node){
				$humidity = $nodosModel->getLastMedition($node->nodo_id,"humedad");
				$node->humidity = $humidity;
			}
			var_dump($nodos);die();
			$this->view('Sensores', array("node_id" => "hola getHumedad"));

		}

	}


?>
